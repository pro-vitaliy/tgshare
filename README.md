# Telegram-бот для хранения и шаринга файлов

Проект, демонстрирующий распределённую, событийно-ориентированную backend-архитектуру на примере Telegram-бота для загрузки и шаринга файлов.

Проект реализует Telegram-бота для загрузки, хранения и предоставления доступа к пользовательским файлам с регистрацией через email и асинхронной обработкой всех пользовательских действий.

---

## 🚀 Ключевые возможности

- 🤖 Telegram-бот с webhook-взаимодействием
- 👤 Регистрация пользователей с подтверждением email
- 📂 Загрузка файлов через Telegram
- 🔗 Генерация ссылок для скачивания и шаринга файлов
- ⚡ Асинхронная, событийно-ориентированная обработка запросов
- 🧩 Микросервисная архитектура

---

## 🧠 Цель проекта

Цель проекта — **продемонстрировать реальные backend-подходы**, которые используются в production-системах:

- микросервисную архитектуру;
- событийное взаимодействие через брокер сообщений;
- работу с кэшем, брокерами, объектным хранилищем;
- разделение ответственности между сервисами;
- проектирование вокруг бизнес-сценариев, а не фреймворков.

---

## 🏗 Обзор архитектуры

Проект состоит из нескольких независимых сервисов, взаимодействующих через RabbitMQ.

**High-level flow:**

1. Пользователь отправляет сообщение или файл в Telegram
2. Telegram отправляет webhook в `Dispatcher`
3. Событие публикуется в RabbitMQ
4. `Node` обрабатывает бизнес-логику
5. Остальные сервисы реагируют на события
6. Ответ пользователю отправляется обратно в Telegram

📄 Подробное архитектурное описание: **[ARCHITECTURE.md](./ARCHITECTURE.md)**

---

## 🧩 Сервисы

| Сервис | Зона ответственности |
|--------|----------------------|
| **Dispatcher** | Приём webhook-запросов от Telegram, маршрутизация апдейтов |
| **Node** | Центральный оркестратор, обработка бизнес-логики |
| **User Service** | Управление пользователями, подтверждение email |
| **File Service** | Обработка и хранение файлов |
| **Notification Service** | Отправка email-уведомлений |
| **Common lib** | Общие DTO и контракты событий |

Каждый сервис — отдельный Maven-проект и может разворачиваться независимо.

---

## 🛠 Технологический стек

**Backend & Infrastructure**
- Java 21+
- Spring Boot
- RabbitMQ
- Redis
- PostgreSQL
- MinIO (S3-compatible storage)
- gRPC
- Docker & Docker Compose

**Other**
- Maven
- Telegram Bot API

---

## 📦 Структура репозитория

```
├── ARCHITECTURE.md # Документация архитектуры проекта
├── docker-compose.yml # Конфигурация локального окружения Docker
├── .env.example # Пример файла переменных окружения
├── Makefile # Утилиты для локального запуска и сборки
├── start.sh # Скрипт запуска всех сервисов
├── services/ # Микросервисы проекта
│ ├── common-lib/ # Общие DTO и контракты событий
│ ├── dispatcher/ # Dispatcher service (webhook gateway)
│ ├── node/ # Node service (основная бизнес-логика)
│ ├── user-service/ # Сервис управления пользователями
│ ├── file-service/ # Сервис работы с файлами и MinIO
│ └── notification-service/ # Сервис отправки email уведомлений
└── README.md # Текущая документация проекта
```

---

## ▶️ Запуск проекта локально

### 🛠 Требования

* Docker & Docker Compose
* Telegram Bot Token

---

### ⚙️ Настройка

Скопируйте пример файла переменных окружения и заполните необходимые значения:

```bash
cp .env.example .env
```

Затем откройте `.env` и укажите реальные токены и другие параметры.

---

### 🚀 Поднятие проекта

Все сервисы можно поднять одной командой через скрипт `start.sh`:

```bash
chmod +x start.sh
./start.sh
```

**Что делает скрипт:**

1. Собирает образ `common-lib` (необходимый для зависимостей между сервисами).
2. Строит и запускает все сервисы через Docker Compose:

* RabbitMQ
* PostgreSQL (для `user-service` и `file-service`)
* Redis
* MinIO
* Dispatcher
* Node
* User Service
* File Service
* Notification Service

> 💡 После первого запуска `common-lib` будет доступна как зависимость в локальном Maven репозитории сервисов.

---

### ✅ Проверка работы

* RabbitMQ Management UI: [http://localhost:15672](http://localhost:15672)
* MinIO Console: [http://localhost:9001](http://localhost:9001)
* Telegram бот: по webhook, настроенному в `.env`

---

### 🔄 Перезапуск или остановка

**Остановить все сервисы:**

```bash
docker-compose down
```

**Перезапустить с пересборкой:**

```bash
./start.sh
```

---

## 🔐 Пользовательский сценарий (кратко)

1. Пользователь запускает Telegram-бота
2. Пользователь автоматически создаётся со статусом guest
3. При попытке загрузки файла требуется регистрация
4. Пользователю отправляется письмо с ссылкой подтверждения
5. После подтверждения email пользователь может загружать файлы
6. Бот возвращает ссылку для скачивания и шаринга файла

---

## 📈 Что демонстрирует проект

- Событийно-ориентированную микросервисную архитектуру (event-driven microservices)
- Асинхронную коммуникацию между сервисами через RabbitMQ
- Использование Redis как read-through cache
- gRPC для внутренних синхронных вызовов между сервисами
- Работа с S3-совместимым объектным хранилищем (MinIO)
- Чёткое разделение ответственности между компонентами системы

---

## 🧪 Тестирование и надёжность

- Юнит и интеграционные тесты для всех сервисов

---

## 🧭 Возможные улучшения

- Экспериментальный перевод части сервисов на виртуальные потоки с анализом влияния на производительность и поведение системы
- Метрики и трассировка (Prometheus, Grafana) для наблюдаемости сервисов
- Ограничение количества запросов и квоты на загрузку файлов
- Подготовка манифестов для развертывания в Kubernetes
- Настройка CI/CD для автоматической сборки и деплоя

---

## 👤 Author

Vitaliy — Java Backend Developer

---

